{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Config",
  "description": "SuperVox configuration",
  "type": "object",
  "properties": {
    "my_language": {
      "type": "string",
      "default": "ru",
      "description": "Target language for summaries and translation"
    },
    "stt_backend": {
      "type": "string",
      "enum": ["realtime", "whisper"],
      "default": "realtime",
      "description": "Speech-to-text backend"
    },
    "llm_model": {
      "type": "string",
      "default": "gemini-2.5-flash",
      "description": "LLM model for analysis and chat"
    },
    "summary_lag_secs": {
      "type": "integer",
      "minimum": 1,
      "default": 5,
      "description": "Seconds between rolling summary updates in live mode"
    },
    "capture": {
      "type": "string",
      "enum": ["mic", "mic+system"],
      "default": "mic+system",
      "description": "Audio capture mode"
    },
    "llm_backend": {
      "type": "string",
      "enum": ["auto", "ollama"],
      "default": "auto",
      "description": "LLM backend: 'auto' for cloud models, 'ollama' for local"
    },
    "ollama_model": {
      "type": "string",
      "default": "llama3.2:3b",
      "description": "Ollama model name when llm_backend is 'ollama'"
    },
    "whisper_model": {
      "type": "string",
      "enum": ["tiny", "base", "small", "medium"],
      "default": "base",
      "description": "Whisper model size for local STT"
    },
    "ducking_threshold": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "default": 0.05,
      "description": "System audio level above which mic STT is suppressed (0.0-1.0)"
    }
  }
}
