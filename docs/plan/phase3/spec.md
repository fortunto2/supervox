# Phase 3 — Acceptance Criteria

- Analysis shows structured sections with styled text
- Clipboard copy works (follow-up and full analysis)
- Agent loads last 10 calls as context with compaction
- Agent search finds relevant calls by query
- Agent generates follow-up emails on demand
- Calls auto-tagged with themes after analysis
- Export to markdown works
- All tests pass, clippy clean
