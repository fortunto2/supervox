# Implementation Plan: Audio Recording Persistence

**Track ID:** audio-save_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

Add incremental WAV recording to the audio pipeline, persist alongside call JSON, and wire playback through CLI and TUI. Three phases: audio save, CLI integration, TUI + docs.

## Phase 1: Audio Save Pipeline

Save mic audio as WAV during live recording using incremental `hound::WavWriter`.

### Tasks

- [x] Task 1.1: Add `audio_path: Option<String>` field to `Call` in `crates/supervox-agent/src/types.rs`. Use `#[serde(default)]` for backward compat. Add test that existing JSON without `audio_path` deserializes correctly. <!-- sha:72b04ff -->
- [x] Task 1.2: Add `audio_path_for_call()` helper to `crates/supervox-agent/src/storage.rs` — given calls_dir and call_id, returns the expected WAV path (`{date}-{id}.wav`). Add `has_audio()` that checks if WAV file exists for a call. Unit tests. <!-- sha:c8ef46a -->
- [x] Task 1.3: Update `delete_call()` in `crates/supervox-agent/src/storage.rs` to also remove `{basename}.wav` if it exists alongside the JSON file. Update existing delete test. <!-- sha:b70c7b1 -->
- [x] Task 1.4: Modify `run_pipeline()` in `crates/supervox-tui/src/audio.rs` — on start, create a `hound::WavWriter<BufWriter<File>>` writing to a temp WAV path. In the mic chunk handler, write raw f32 samples as i16 to the WAV writer (same conversion as `AudioChunk::to_wav_bytes`). On stop, finalize the writer. <!-- sha:6d64ff4 -->
- [x] Task 1.5: Extend `AudioEvent::Stopped` with `audio_path: Option<String>` — the path to the saved WAV. Update `save_recorded_call()` to accept and store `audio_path` in the `Call` struct. <!-- sha:6d64ff4 -->

### Verification

- [x] `cargo test -p supervox-agent` passes with new backward-compat and audio_path tests
- [x] Recording in live mode creates both `.json` and `.wav` files in `~/.supervox/calls/`
- [x] WAV file is playable (`afplay` or `open` on macOS)

## Phase 2: CLI Integration

Wire audio into CLI commands: play, calls list indicator, delete cleanup.

### Tasks

- [x] Task 2.1: Add `Play` subcommand to `crates/supervox-tui/src/main.rs` — `supervox play <call-id>`. Finds call, checks `has_audio()`, opens WAV with `open` (macOS) via `std::process::Command`. Error if no audio. <!-- sha:5911451 -->
- [x] Task 2.2: Update `cmd_calls()` to show audio indicator — `♫` if WAV exists, blank if not. Add column to the header. <!-- sha:6584ac9 -->
- [x] Task 2.3: Update `export_call_markdown()` in `crates/supervox-agent/src/storage.rs` to include `**Audio:** {filename}` line when `call.audio_path` is set. <!-- sha:fc4cc40 -->

### Verification

- [x] `supervox play <call-id>` opens audio in system player
- [x] `supervox calls` shows ♫ indicator for calls with audio
- [x] `supervox delete <call-id>` removes both JSON and WAV files

## Phase 3: TUI + Docs

Show audio status in Analysis mode and update documentation.

### Tasks

- [x] Task 3.1: Update Analysis mode rendering in `crates/supervox-tui/src/modes/analysis.rs` — show "Audio: ♫ (press 'p' to play)" or "Audio: none" in the header area. Handle 'p' keypress to open WAV via system player. <!-- sha:31e9781 -->
- [x] Task 3.2: Update CLAUDE.md — add `audio_path` field to Call docs, `supervox play` command, 'p' keybinding in Analysis mode, WAV storage notes. <!-- sha:5328800 -->
- [x] Task 3.3: Update README.md — add `supervox play` to usage, note audio recording in Live mode section. <!-- sha:3c70bd1 -->
- [x] Task 3.4: Remove dead code — unused imports, stale comments. Run `cargo clippy --workspace -- -D warnings` and `cargo fmt --all`. <!-- sha:3c70bd1 -->

### Verification

- [x] Analysis mode shows audio status and 'p' to play works
- [x] CLAUDE.md and README.md reflect new features
- [x] Linter clean, tests pass

## Final Verification

- [x] All acceptance criteria from spec met
- [x] Tests pass
- [x] Linter clean
- [x] Build succeeds
- [x] Documentation up to date

---
_Generated by /plan. Tasks marked [~] in progress and [x] complete by /build._
