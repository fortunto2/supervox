# Phase 2 — Acceptance Criteria

- Live mode uses WebSocket streaming STT (not batch)
- Partial transcripts shown in real-time (< 500ms latency)
- Translation appears below each completed utterance
- Rolling summary updates every 5s in right panel
- System audio + mic captured simultaneously (macOS)
- Audio level meter visible in status bar
- Call timer shows elapsed time
- Config loaded from `~/.supervox/config.toml`
- All tests pass, clippy clean
