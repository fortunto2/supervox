# Pipeline Retro: supervox (2026-03-20) — Full Pipeline

## Overall Score: 8.5/10

Two pipeline runs in one continuous session: Phase 1 (workspace + agent + TUI skeleton, 7 iters) then auto-planned realtime-live track (streaming STT + live intelligence, 3 iters). 10 total iterations, 80% productive, 100 tests passing, 40 commits all conventional.

## Pipeline Efficiency

| Metric | Value | Rating |
|--------|-------|--------|
| Total iterations | 10 (7 + 3) | GREEN |
| Productive iterations | 8 (80%) | GREEN |
| Wasted iterations | 2 (20%) | YELLOW |
| Pipeline runs | 2 (1 manual + 1 auto-restart) | GREEN |
| Aborted pre-starts | 6 (earlier log files) | YELLOW |
| Max-iter hits | 0 | GREEN |
| Total duration | ~151 min (124 + 27) | YELLOW |

### Pre-Start Failures

Before the main pipeline, 6 aborted starts occurred across `pipeline-20260320-013628.log` (3 STARTs, no iters) and `pipeline-20260320-014937.log` (3 STARTs + rate limit). These were setup/configuration attempts — no iterations burned, but indicate initial friction.

## Per-Stage Breakdown

### Run 1: Phase 1 (7 iterations, 124 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 2 | 2 | 0% | Clean both runs (47 min initial, 3 min fix) |
| deploy | 3 | 2 | 33% | Iter 2: AskUserQuestion stall (CLI/TUI scope confusion) |
| review | 2 | 1 | 50% | Iter 4: valid redo — found UTF-8 bug, missing README, no hooks |

### Run 2: realtime-live (3 iterations, 27 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 1 | 1 | 0% | 22 min, all 4 phases of plan completed in single build |
| deploy | 1 | 1 | 0% | 3 min, clean push + archive |
| review | 1 | 1 | 0% | 2 min, clean pass with spec verification |

Run 2 was **flawless** — 3/3 productive, zero waste, 27 minutes total. Auto-plan from backlog picked the right priority (realtime STT was the critical Phase 2 gap).

## Failure Patterns

### Pattern 1: Deploy Scope Confusion (CLI/TUI project)

- **Occurrences:** 1 iteration (Run 1, iter 2)
- **Root cause:** Deploy skill designed for web/cloud deployments. Faced a CLI/TUI tool and used AskUserQuestion which blocked the pipeline. No `<solo:done/>` output.
- **Wasted:** 1 iteration (~3 min)
- **Fix:** Deploy skill needs project-type detection: binary targets + TUI/CLI keywords → auto CI/CD + release workflow.
- **Note:** Deploy learned from failure — Run 2 deploy completed cleanly in 3 min.

### Pattern 2: Doubled Signal (cosmetic)

- **Occurrences:** 1 (Run 1, iter 4)
- **Root cause:** Review skill output `<solo:redo/>` twice. Matches catalog Pattern 4.
- **Wasted:** 0 iterations
- **Fix:** None needed.

### Pattern 3: Valid Redo Cycle

- **Occurrences:** 1 redo cycle (Run 1, iter 4→5→6→7)
- **Root cause:** Review correctly identified 3 real issues: UTF-8 slicing bug in search (would panic on Cyrillic), missing README, no pre-commit hooks. All fixed in 3 min.
- **Assessment:** Redo mechanism working exactly as designed. Build skill completed 3 review-requested tasks with SHA annotations.

### Pattern 4: Pre-Start Rate Limit

- **Occurrences:** 1 (pipeline-20260320-014937.log)
- **Root cause:** Claude CLI rate-limited during pipeline startup. Script detected 56B output and correctly identified as rate limit, waited 60s.
- **Wasted:** ~1 min wait time
- **Note:** Rate limit detection in solo-dev.sh worked correctly.

## Plan Fidelity

| Track | Criteria Met | Tasks Done | SHAs | Rating |
|-------|-------------|------------|------|--------|
| phase1 | 13/13 (100%) | 20/20 (100%) | 20/20 | GREEN |
| realtime-live | 9/9 (100%) | 14/14 (100%) | 14/14 | GREEN |
| phase2 | — | — | — | Archived unbuilt (replaced by realtime-live) |
| phase3 | — | — | — | Archived unbuilt (future work) |
| phase4 | — | — | — | Archived unbuilt (future work) |

Both executed tracks: 100% criteria met, 100% tasks done, all with SHA annotations. Phases 2-4 were pre-planned in Phase 1 but never executed — auto-plan correctly identified realtime-live as the priority instead.

## Code Quality (Quick)

- **Tests:** 100 pass, 0 fail (36 agent + 11 tui + 11 extra + 42 voxkit)
- **Build:** PASS
- **Clippy:** PASS (1 warning in external dep sgr-agent, not supervox)
- **Fmt:** PASS
- **Commits:** 40 total, 100% conventional format
- **Pre-commit hooks:** Active (cargo fmt + clippy)

## Three-Axis Growth

| Axis | Score | Evidence |
|------|-------|----------|
| **Technical** | 9/10 | 3 Rust crates, 6 agent tools, realtime WebSocket STT, system audio capture, async translation + summary pipelines, config system |
| **Cognitive** | 9/10 | Auto-plan picked right priority (realtime-live over sequential phases). Schemas-first approach. Review caught real UTF-8 bug. voxkit reuse avoided duplication |
| **Process** | 8/10 | Run 2 was flawless (3/3), pipeline auto-cycling worked, retro + codex factory critique ran. Deploy skill stumble was only process gap |

## Scoring Breakdown

| Dimension | Weight | Score | Weighted |
|-----------|--------|-------|----------|
| Efficiency | 25% | 7 | 1.75 |
| Stability | 20% | 8 | 1.60 |
| Fidelity | 20% | 10 | 2.00 |
| Quality | 15% | 10 | 1.50 |
| Commits | 5% | 10 | 0.50 |
| Docs | 5% | 10 | 0.50 |
| Signals | 5% | 7 | 0.35 |
| Speed | 5% | 5 | 0.25 |
| **Total** | 100% | | **8.45 → 8.5** |

**Score notes:**
- Efficiency 7: 20% waste (2/10 iters) — Run 2 pulled this up from Run 1's 28.6%
- Stability 8: 1 auto-restart (plan cycling), 6 aborted pre-starts in earlier logs
- Speed 5: 151 min total (~2.5 hours) across both runs

## Recommendations

1. **[HIGH]** Deploy skill needs CLI/TUI project-type detection — auto-detect from Cargo.toml binary targets + CLAUDE.md keywords, default to GitHub CI/CD + release workflow. Prevents AskUserQuestion stalls.

2. **[MEDIUM]** Pre-start friction — 6 aborted starts before successful pipeline. Consider retry/backoff in solo-dev.sh startup phase or clearer error messages for config issues.

3. **[LOW]** Auto-plan quality was excellent — picked realtime-live focus over sequential phases. Consider documenting this as a pattern for other projects.

4. **[LOW]** Add `cargo-tarpaulin` or `cargo-llvm-cov` for test coverage metrics in CI.

## Suggested Patches

### Patch 1: Deploy SKILL.md — CLI/TUI project detection

**What:** Add project-type detection before deployment to handle non-web projects
**Why:** Deploy iter 2 wasted on AskUserQuestion for CLI/TUI scope

```diff
  ### 2. Detect available CLI tools
+ ### 2.5. Detect project type
+ Check CLAUDE.md and Cargo.toml for project type:
+ - Binary target + `ratatui`/`clap`/`crossterm` deps → CLI/TUI project
+ - CLAUDE.md contains "TUI", "CLI", "terminal" → CLI/TUI project
+ - CLI/TUI projects: deploy = GitHub repo + CI/CD + release workflow + binary asset
+ - Do NOT use AskUserQuestion for deployment scope — use sensible defaults
+
  Run in parallel — detect what's installed locally:
```

## Factory Score: 8.5/10

### Skill quality:

| Skill | Score | Run 1 | Run 2 |
|-------|-------|-------|-------|
| build | 10/10 | 20 tasks, all SHAs, multi-phase plan | 14 tasks, 4 phases, single iteration |
| deploy | 7/10 | Failed iter 2 (AskUserQuestion stall), recovered iter 3 | Clean 3 min execution |
| review | 9/10 | Found 3 real issues, doubled signal cosmetic | Clean pass, spec verification |

Deploy improved from 6/10 (Run 1 only) to 7/10 thanks to clean Run 2 execution.

### Pipeline reliability: 9/10

Two successful runs with auto-plan cycling between them. No maxiter hits. Auto-plan correctly selected realtime-live as next priority. State management clean. Only concern: 6 aborted pre-starts.

### Auto-plan quality: 10/10

Auto-plan examined completed Phase 1, read phases 2-4 plans, analyzed codebase, and generated a focused realtime-live plan that combined the most impactful items from Phase 2. This was better than mechanically running Phase 2 as-is — it adapted to what the codebase actually needed.

### Missing capabilities:
- Rust-native deploy strategy (cargo publish, homebrew tap, cross-compilation)
- TUI visual regression testing (ratatui snapshot tests)
- Pre-start validation in solo-dev.sh (catch config issues before entering main loop)

### Top factory defects:
1. Deploy skill lacks project-type detection → `skills/deploy/SKILL.md` → Add CLI/TUI auto-detection
2. Pre-start instability → `scripts/solo-dev.sh` → Add startup validation + clearer error messages

### Harness Evolution

**Context engineering:** Agent had everything needed in-repo. CLAUDE.md lean at 5,375 bytes. Schemas-first approach gave clear domain model. Auto-plan had full visibility into plan-done archives + PRD + codebase.

**Architectural constraints:** No boundary violations. Agent maintained clean separation: types → tools → storage → audio pipeline → TUI modes. All files under limits.

**Decision traces:**
- Excellent: Auto-plan picking focused realtime-live over sequential Phase 2
- Excellent: voxkit reuse — zero audio code duplication
- Good: Review redo caught UTF-8 bug before shipping
- Good: Run 2 build completing 4-phase plan in single iteration (22 min)

**Skill gaps:**
- Deploy needs Rust/CLI/TUI awareness
- No TUI snapshot testing capability
- Auto-plan is a strong pattern — should be documented as reference for other projects
