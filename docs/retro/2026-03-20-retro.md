# Pipeline Retro: supervox (2026-03-20) — Final (6 Runs)

## Overall Score: 9.2/10

Six pipeline runs in one continuous session: Phase 1 (workspace + agent + TUI skeleton, 7 iters, 124 min), realtime-live (streaming STT + live intelligence, 3 iters, 32 min), analysis-agent-wire (LLM wiring for analysis + agent modes, 3 iters, 28 min), ux-polish (help overlay, history browser, JSON output, Ollama config, 3 iters, 32 min), call-management (delete, export, search, 3 iters, 23 min), call-insights (analysis persistence + cross-call insights, 3 iters, 25 min). 22 total iterations, 90.9% productive, 94 commits all conventional.

## Pipeline Efficiency

| Metric | Value | Rating |
|--------|-------|--------|
| Total iterations | 22 (7 + 3 + 3 + 3 + 3 + 3) | GREEN |
| Productive iterations | 20 (90.9%) | GREEN |
| Wasted iterations | 2 (9.1%) | GREEN |
| Pipeline runs | 6 (1 manual + 5 auto-restarts) | GREEN |
| Aborted pre-starts | 6 (earlier log files) | YELLOW |
| Max-iter hits | 0 | GREEN |
| Total duration | ~264 min (124 + 32 + 28 + 32 + 23 + 25) | YELLOW |

### Pre-Start Failures

Before the main pipeline, 6 aborted starts occurred across `pipeline-20260320-013628.log` (3 STARTs, no iters) and `pipeline-20260320-014937.log` (3 STARTs + rate limit). These were setup/configuration attempts — no iterations burned, but indicate initial friction.

## Per-Stage Breakdown

### Run 1: Phase 1 (7 iterations, 124 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 2 | 2 | 0% | Clean both runs (47 min initial, 3 min fix) |
| deploy | 3 | 2 | 33% | Iter 2: AskUserQuestion stall (CLI/TUI scope confusion) |
| review | 2 | 1 | 50% | Iter 4: valid redo — found UTF-8 bug, missing README, no hooks |

### Run 2: realtime-live (3 iterations, 32 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 1 | 1 | 0% | 22 min, all 4 phases of plan completed in single build |
| deploy | 1 | 1 | 0% | 3 min, clean push + archive |
| review | 1 | 1 | 0% | 2 min, clean pass with spec verification |

### Run 3: analysis-agent-wire (3 iterations, 28 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 1 | 1 | 0% | 16 min, 4-phase plan (analysis + clipboard + agent + docs) |
| deploy | 1 | 1 | 0% | 3 min, Cargo.lock + plan archive + push |
| review | 1 | 1 | 0% | 3 min, clean pass — all 10 acceptance criteria verified |

### Run 4: ux-polish (3 iterations, 32 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 1 | 1 | 0% | 22 min, 4 phases (help overlay + history browser + CLI + docs) |
| deploy | 1 | 1 | 0% | 3 min, version bump + Cargo.lock update |
| review | 1 | 1 | 0% | 2 min, clean pass — all 8 acceptance criteria verified |

### Run 5: call-management (3 iterations, 23 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 1 | 1 | 0% | 13 min, 4 phases (storage + CLI + TUI + docs) |
| deploy | 1 | 1 | 0% | 2 min, clean push + archive |
| review | 1 | 1 | 0% | 2 min, clean pass — all 11 acceptance criteria verified |

### Run 6: call-insights (3 iterations, 25 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 1 | 1 | 0% | 20 min, 3 phases (persist analysis + insights CLI + docs) |
| deploy | 1 | 1 | 0% | 2 min, clean push |
| review | 1 | 1 | 0% | 3 min, clean pass — all 10 acceptance criteria verified |

Runs 2-6 were **flawless** — 15/15 productive, zero waste. Auto-plan from backlog picked correct priorities: realtime-live → analysis-agent-wire → ux-polish → call-management → call-insights. Each successive run was faster and cleaner.

## Failure Patterns

### Pattern 1: Deploy Scope Confusion (CLI/TUI project)

- **Occurrences:** 1 iteration (Run 1, iter 2)
- **Root cause:** Deploy skill designed for web/cloud deployments. Faced a CLI/TUI tool and used AskUserQuestion which blocked the pipeline. No `<solo:done/>` output.
- **Wasted:** 1 iteration (~3 min)
- **Fix:** Deploy skill needs project-type detection: auto-detect from Cargo.toml binary targets + CLAUDE.md keywords, default to GitHub CI/CD + release workflow.
- **Note:** Deploy learned from failure — Runs 2-6 completed cleanly (2-3 min each).

### Pattern 2: Doubled Signal (cosmetic)

- **Occurrences:** 1 (Run 1, iter 4)
- **Root cause:** Review skill output `<solo:redo/>` twice. Matches catalog Pattern 4.
- **Wasted:** 0 iterations
- **Fix:** None needed.

### Pattern 3: Valid Redo Cycle

- **Occurrences:** 1 redo cycle (Run 1, iter 4→5→6→7)
- **Root cause:** Review correctly identified 3 real issues: UTF-8 slicing bug in search (would panic on Cyrillic), missing README, no pre-commit hooks. All fixed in 3 min.
- **Assessment:** Redo mechanism working exactly as designed. Build skill completed 3 review-requested tasks with SHA annotations.

### Pattern 4: Pre-Start Rate Limit

- **Occurrences:** 1 (pipeline-20260320-014937.log)
- **Root cause:** Claude CLI rate-limited during pipeline startup.
- **Wasted:** ~1 min wait time
- **Note:** Rate limit detection in solo-dev.sh worked correctly.

## Plan Fidelity

| Track | Criteria Met | Tasks Done | SHAs | Rating |
|-------|-------------|------------|------|--------|
| phase1 | 16/16 (100%) | 22/22 (100%) | 22/22 | GREEN |
| realtime-live | 9/9 (100%) | 14/14 (100%) | 14/14 | GREEN |
| analysis-agent-wire | 10/10 (100%) | 16/16 (100%) | 16/16 | GREEN |
| ux-polish | 8/8 (100%) | 17/17 (100%) | 17/17 | GREEN |
| call-management | 11/11 (100%) | 14/14 (100%) | 14/14 | GREEN |
| call-insights | 10/10 (100%) | 13/13 (100%) | 13/13 | GREEN |
| phase2-4 | -- | -- | -- | Archived (superseded by auto-plan tracks) |

All six executed tracks: 100% criteria met, 100% tasks done (96/96), all with SHA annotations. Original phases 2-4 were broad plans superseded by focused auto-generated tracks.

## Code Quality (Quick)

- **Tests:** PASS (0 failures across workspace)
- **Build:** PASS
- **Clippy:** PASS (1 warning in external dep sgr-agent, not supervox)
- **Fmt:** PASS
- **Commits:** 94 total, 100% conventional format
- **Committers:** 1 (fortunto2 — all 94 commits)
- **Pre-commit hooks:** Active (cargo fmt + clippy)
- **CLAUDE.md:** 8,843 bytes (well under 40K limit)

## Three-Axis Growth

| Axis | Score | Evidence |
|------|-------|----------|
| **Technical** | 10/10 | 3 Rust crates, 6 agent tools, realtime WebSocket STT, system audio capture, async translation + summary + analysis pipelines, agent mode with streaming LLM, clipboard, config system, help overlay, call history browser, Ollama support, JSON CLI output, error resilience, call delete/export/search, analysis persistence, cross-call insights |
| **Cognitive** | 10/10 | Auto-plan evolved plans from broad phases to focused tracks (5 successful evolutions). Schemas-first approach. Review caught real UTF-8 bug. voxkit reuse avoided duplication. Each auto-plan examined completed work and generated the optimal next track. Call-insights track correctly identified ephemeral analysis as waste. |
| **Process** | 10/10 | Runs 2-6 all flawless (15/15 productive). Pipeline auto-cycling worked across 6 runs. Progressive improvement: 71% → 100% × 5. CLAUDE.md stayed lean at 8.8K. |

## Scoring Breakdown

| Dimension | Weight | Score | Weighted |
|-----------|--------|-------|----------|
| Efficiency | 25% | 8 | 2.00 |
| Stability | 20% | 10 | 2.00 |
| Fidelity | 20% | 10 | 2.00 |
| Quality | 15% | 10 | 1.50 |
| Commits | 5% | 10 | 0.50 |
| Docs | 5% | 10 | 0.50 |
| Signals | 5% | 10 | 0.50 |
| Speed | 5% | 4 | 0.20 |
| **Total** | 100% | | **9.2** |

**Score notes:**
- Efficiency 8: 9.1% waste (2/22 iters) — improved from 10.5% with Run 6's clean execution
- Stability 10: 0 failure-restarts, 0 maxiter. 6 STARTs are sequential auto-plan runs, not failure recovery
- Signals 10: Runs 2-6 had perfect clean signals. Run 1 minor cosmetic doubled redo not penalized
- Speed 4: ~264 min total (~4h 24m). Individual runs: 124m → 32m → 28m → 32m → 23m → 25m. Per-track average: 44 min for 6 complete feature tracks — efficient for the scope.

## Recommendations

1. **[HIGH]** Deploy skill needs CLI/TUI project-type detection — auto-detect from Cargo.toml binary targets + CLAUDE.md keywords, default to GitHub CI/CD + release workflow. Prevents AskUserQuestion stalls.

2. **[MEDIUM]** Pre-start friction — 6 aborted starts before successful pipeline. Consider retry/backoff in solo-dev.sh startup phase or clearer error messages for config issues.

3. **[LOW]** Add `cargo-tarpaulin` or `cargo-llvm-cov` for test coverage metrics in CI.

4. **[LOW]** Consider splitting test suite into `make test-fast` (unit) and `make test-full` (integration) for faster iteration.

## Suggested Patches

### Patch 1: Deploy SKILL.md — CLI/TUI project detection

**What:** Add project-type detection before deployment to handle non-web projects
**Why:** Deploy iter 2 wasted on AskUserQuestion for CLI/TUI scope

```diff
  ### 2. Detect available CLI tools
+ ### 2.5. Detect project type
+ Check CLAUDE.md and Cargo.toml for project type:
+ - Binary target + `ratatui`/`clap`/`crossterm` deps -> CLI/TUI project
+ - CLAUDE.md contains "TUI", "CLI", "terminal" -> CLI/TUI project
+ - CLI/TUI projects: deploy = GitHub repo + CI/CD + release workflow + binary asset
+ - Do NOT use AskUserQuestion for deployment scope — use sensible defaults
+
  Run in parallel — detect what's installed locally:
```

## Factory Score: 9.2/10

### Skill quality:

| Skill | Score | Summary |
|-------|-------|---------|
| build | 10/10 | 96 tasks across 6 runs, all with SHAs, 100% completion. Single-iter in runs 2-6. |
| deploy | 8/10 | Failed iter 2 in Run 1 (AskUserQuestion stall). Clean in Runs 2-6 (2-3 min each). |
| review | 10/10 | Valid redo in Run 1 caught real bug. Clean pass in Runs 2-6, all criteria verified. |
| auto-plan | 10/10 | 5 successful plan evolutions, each progressively focused and faster. |

### Pipeline reliability: 10/10

Six successful runs with auto-plan cycling. Zero maxiter hits. Auto-plan correctly selected priorities: realtime-live → analysis-agent-wire → ux-polish → call-management → call-insights. State management clean throughout.

### Missing capabilities:
- Rust-native deploy strategy (cargo publish, homebrew tap, cross-compilation)
- TUI visual regression testing (ratatui snapshot tests)
- Pre-start validation in solo-dev.sh

### Top factory defects:
1. Deploy skill lacks project-type detection → `skills/deploy/SKILL.md` → Add CLI/TUI auto-detection
2. Pre-start instability → `scripts/solo-dev.sh` → Add startup validation + clearer error messages

### Harness Evolution

**Context engineering:** Agent had everything needed in-repo. CLAUDE.md grew from 5,375 to 8,843 bytes as features were documented — lean and actionable. Schemas-first gave clear domain model. Auto-plan had full visibility into plan-done archives + PRD + codebase.

**Architectural constraints:** No boundary violations across all 6 runs. Agent maintained clean separation: types → tools → storage → audio pipeline → intelligence → analysis_pipeline → agent_loop → TUI modes. All files under limits.

**Decision traces:**
- Excellent: Auto-plan evolving from broad phases to focused tracks (5 successful evolutions)
- Excellent: voxkit reuse — zero audio code duplication across all 6 runs
- Excellent: Wiring strategy (existing tools → async pipelines → app events → UI) was clean and consistent
- Excellent: call-insights track identified ephemeral analysis as waste and added persistence + cross-call intelligence
- Good: Review redo in Run 1 caught real UTF-8 bug before shipping
- Good: Progressive test growth with each track adding relevant tests without bloat

**Skill gaps:**
- Deploy needs Rust/CLI/TUI awareness
- No TUI snapshot testing capability
- Auto-plan pattern is strong — should be documented as reference for other projects
