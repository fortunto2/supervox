# Pipeline Retro: supervox (2026-03-20) — Final (18 Runs, 16 Tracks)

## Overall Score: 7.9/10

Eighteen pipeline runs across four sessions. 16 tracks fully built, deployed, and reviewed — from v0.1.0 to v0.7.0 (audio import). 50 total iterations, 94% productive (3 wasted). 183 commits, 100% conventional format, 263 tests passing (up from 55 at live-ux), clippy clean. Two state leak incidents from same factory bug (solo-dev.sh:939 — **4th flagging, still unfixed**). One redo cycle that caught a real UTF-8 bug. One 6h global timeout. Latest run (audio-import) was flawless: 3/3 iters, 40 min, 100% fidelity.

## Pipeline Efficiency

| Metric | Value | Rating |
|--------|-------|--------|
| Total iterations | 50 | GREEN |
| Productive iterations | 47 (94%) | GREEN |
| Wasted iterations | 3 (6%) | GREEN |
| Pipeline runs | 18 (auto-cycling + 2 manual restarts) | GREEN |
| State leak incidents | 2 (action-tracker-v1, live-ux-v1) | RED |
| Max-iter hits | 0 | GREEN |
| Redo cycles | 1 (Run 1 only) | GREEN |
| Plans cycled | 18 total | GREEN |
| Global timeout | 1 (6h limit) | YELLOW |
| Total wall time | ~17h (01:51–18:40, includes 3h gap) | RED |
| Per-track avg | ~35 min (16 built tracks) | GREEN |

## Per-Stage Breakdown

### Run 1: Phase 1-4 (7 iterations, 124 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 2 | 2 | 0% | 47 min initial, 3 min fix after redo |
| deploy | 3 | 2 | 33% | Iter 2: AskUserQuestion stall (CLI/TUI scope confusion) |
| review | 2 | 1 | 50% | Iter 4: valid redo — found UTF-8 bug, missing README |

### Runs 2-8: Feature Tracks (3 iters each, 22-32 min)

| Run | Track | Duration | Notes |
|-----|-------|----------|-------|
| 2 | realtime-live | 32 min | Streaming STT + live intelligence |
| 3 | analysis-agent-wire | 28 min | LLM wiring for analysis + agent |
| 4 | ux-polish | 32 min | Help overlay, history, JSON output, Ollama |
| 5 | call-management | 23 min | Delete, export, search |
| 6 | call-insights | 30 min | Analysis persistence + cross-call insights |
| 7 | call-stats | 23 min | Aggregate stats + batch analysis |
| 8 | call-filter | 26 min | Tag + date range filtering |

All 7 runs: **3/3 productive, 0% waste**. 21/21 iterations successful.

**TIMEOUT at 07:53** — 6h global limit reached after Run 8. State markers NOT cleaned.

### Run 9: action-tracker-v1 (0 iterations, 5 min) — STATE LEAK

Stale `.solo/states/` markers from timeout made pipeline think track was already complete. Falsely archived with 0% completion. **Root cause:** solo-dev.sh:939 — no state cleanup on timeout.

### Run 10: audio-save (3 iterations, 48 min)

3/3 productive. WAV recording pipeline + system audio capture, v0.3.0 release.

### Run 11: action-tracker-v2 (3 iterations, 28 min) — RECOVERY

Rebuilt after state leak in Run 9. 3/3 productive. Action tracking types + CLI + TUI, v0.3.1 release.

### Run 12: live-bookmarks (3 iterations, 29 min)

3/3 productive. Bookmark types + TUI wiring, v0.4.0 release. 8/9 criteria (1 minor CLI gap).

### Run 13: whisper-stt (3 iterations, 74 min) — MOST COMPLEX

| Stage | Duration | Notes |
|-------|----------|-------|
| build | 39 min | 4 phases: trait abstraction + whisper backend + TUI wiring + docs. New C++ dep. |
| deploy | 26 min | v0.5.0 release. CI fix: dropped x86_64 (ort-sys no prebuilt). LTO + strip. |
| review | 9 min | SHIP verdict, 9/9 criteria verified. |

### Run 14: live-ux-v1 (0 iterations) — STATE LEAK (RECURRING)

Stale state markers from whisper-stt (Run 13). Same factory bug as Run 9. Retro only, no build.

### Runs 15-16: live-ux-v2 (4 iterations, ~69 min) — RECOVERY + COMPLETION

| Iter | Stage | Result | Notes |
|------|-------|--------|-------|
| 1 | build | continuing | First attempt — no `<solo:done/>` signal. Tasks partially done. |
| 1 (restart) | build | stage complete | 10 min. Completed remaining tasks + marked plan done. |
| 2 | deploy | stage complete | 41 min. v0.6.0 release. 55 tests, LTO binary. |
| 3 | review | stage complete | 7 min. SHIP verdict. |

live-ux features delivered: dual VU meters, mic ducking, Space/Enter keybindings. **However, spec/plan checkboxes NOT ticked** — fidelity gap (code delivered, docs lagging).

### Run 17: quality-hardening (3 iterations, 28 min) — FLAWLESS

| Stage | Duration | Notes |
|-------|----------|-------|
| build | 19 min | 3 phases: typed enums + config validation + unwrap cleanup. 8/8 tasks. |
| deploy | 4 min | v0.6.1 release. No CI issues. |
| review | 5 min | SHIP verdict. 10/10 criteria verified, all checkboxes ticked. |

3/3 productive, 0% waste. Tests grew from 55 to 256. All spec + plan checkboxes properly ticked.

### Run 18: audio-import (3 iterations, 40 min) — FLAWLESS

| Stage | Duration | Notes |
|-------|----------|-------|
| build | 32 min | 3 phases: batch STT methods + CLI import command + docs. 9 files, 416 lines. |
| deploy | 4 min | v0.7.0 release. No CI issues. |
| review | 4 min | SHIP verdict. 9/9 criteria verified, all checkboxes ticked. |

3/3 productive, 0% waste. Tests grew from 256 to 263 (+7). 9/9 acceptance criteria with SHAs on all phases. Checkbox fidelity perfect — pattern from quality-hardening held.

## Failure Patterns

### Pattern 1: State Leak on Timeout/Restart (CRITICAL — RECURRING x2, FLAGGED x4)

- **Occurrences:** 2 (Run 9: action-tracker-v1, Run 14: live-ux-v1)
- **Root cause:** `solo-dev.sh:939` — TIMEOUT branch does not clean state markers. State persists, next plan cycle falsely detects completion.
- **Wasted:** 2 tracks falsely archived. Both recovered via manual re-run but cost extra time.
- **Fix:** Add `rm -f "$STATES_DIR/build" "$STATES_DIR/deploy" "$STATES_DIR/review"` after TIMEOUT log at line 939.
- **Status:** **STILL NOT FIXED** after being identified in Run 9. This is the **4th flagging**.

### Pattern 2: Spec/Plan Checkbox Fidelity Gap (RESOLVED)

- **Occurrences:** 2 tracks (live-ux, action-tracker outer dir)
- **Root cause:** Build marked plan "complete" via commit message but did not tick individual checkboxes.
- **Status:** **RESOLVED** — quality-hardening and audio-import both had perfect checkbox fidelity. Pattern appears corrected in latest runs.

### Pattern 3: Deploy Scope Confusion (Run 1 only — SELF-CORRECTED)

- **Occurrences:** 1 iteration (Run 1, iter 2)
- **Root cause:** Deploy skill designed for web/cloud. Used AskUserQuestion on CLI/TUI project.
- **Self-corrected:** Deploy clean in all 17 subsequent deploys.

### Pattern 4: Valid Redo Cycle (working as designed)

- **Occurrences:** 1 (Run 1, iters 4-7)
- **Root cause:** Review correctly identified UTF-8 slicing bug + missing README.
- **Assessment:** Quality catch, not waste. Redo mechanism working correctly.

### Pattern 5: CI Cross-Compile Failure (Run 13)

- **Occurrences:** 1 (whisper-stt deploy)
- **Root cause:** `ort-sys` no prebuilt binary for x86_64-apple-darwin.
- **Impact:** Deploy took 26 min (vs ~8 min avg) to diagnose and fix.
- **Resolution:** Dropped x86_64 target. Correct pragmatic decision.

### Pattern 6: Build Signal Miss (Run 15)

- **Occurrences:** 1 (live-ux first build attempt)
- **Root cause:** Build completed partial work but did not emit `<solo:done/>`. Likely context exhaustion before reaching signal phase.
- **Impact:** 1 wasted iteration (~10 min), required pipeline restart.
- **Fix:** Build skill should emit progress signals earlier, not just at end.

## Plan Fidelity

| Track | Criteria Met | Tasks Done | SHAs | Rating |
|-------|-------------|------------|------|--------|
| phase1 | 16/16 (100%) | 22/22 (100%) | yes | GREEN |
| realtime-live | 9/9 (100%) | 32/32 (100%) | yes | GREEN |
| analysis-agent-wire | 10/10 (100%) | 36/36 (100%) | yes | GREEN |
| ux-polish | 8/8 (100%) | 38/38 (100%) | yes | GREEN |
| call-management | 11/11 (100%) | 33/33 (100%) | yes | GREEN |
| call-insights | 10/10 (100%) | 28/28 (100%) | yes | GREEN |
| call-stats | 8/8 (100%) | 29/29 (100%) | yes | GREEN |
| call-filter | 12/12 (100%) | 35/35 (100%) | yes | GREEN |
| audio-save | 10/10 (100%) | 26/26 (100%) | yes | GREEN |
| action-tracker | 10/10 (100%) | 24/24 (100%) | yes | GREEN |
| live-bookmarks | 8/9 (89%) | 28/28 (100%) | yes | YELLOW |
| whisper-stt | 9/9 (100%) | 33/33 (100%) | yes | GREEN |
| live-ux | 0/9 (0%) checkbox / 9/9 code | 0/31 checkbox | no | RED (docs) |
| quality-hardening | 10/10 (100%) | 8/8 (100%) | yes | GREEN |
| audio-import | 9/9 (100%) | 10/10 (100%) | yes | GREEN |

**14 tracks: 141/142 criteria (99.3%) with checked specs and SHAs.**
**live-ux: all 9 criteria met in code (commits prove it) but spec/plan checkboxes not ticked.**
**audio-import: perfect fidelity — 100% criteria, 100% tasks, SHAs on all 3 phases.**

## Code Quality (Quick)

- **Tests:** 263 pass, 0 fail, 2 ignored (`cargo test --workspace`) — breakdown: voxkit 108, supervox-agent 42, supervox-tui 42+12 integration, 59 other
- **Build:** PASS (debug + release)
- **Clippy:** PASS (2 warnings in shared dep `sgr-agent`, not supervox workspace)
- **Fmt:** PASS
- **Commits:** 183 total, 100% conventional format
- **Committer:** Single (fortunto2, automated pipeline)

## Context Health

- **Iteration quality trend:** STABLE — runs 2-13, 17-18 all 3/3 clean. Run 15 had 1 miss (signal exhaustion), Run 16 recovered.
- **Observation masking:** NOT USED — no `scratch/` directory. Iter logs manageable.
- **Plan recitation:** OBSERVED — build read plan.md + spec.md at task boundaries (confirmed in audio-import iter-001 log).
- **CLAUDE.md size:** 14,058 chars — OK (well under 40K limit).

## Three-Axis Growth

| Axis | Score | Evidence |
|------|-------|----------|
| **Technical** | 10/10 | 3 Rust crates, 263 tests (up from 55), typed config enums, config validation, unwrap() cleanup, 7 agent tools, realtime + local Whisper STT, Metal acceleration, VAD, StreamingSttBackend trait, system audio capture, WAV recording, async pipelines, streaming LLM, 15 CLI commands (new: import), action tracking, live bookmarks, tag/date filtering, dual VU meters, mic ducking, Space/Enter keybindings, batch file transcription (WAV/MP3/M4A/FLAC/OGG/WebM) |
| **Cognitive** | 9/10 | Auto-plan evolved 16 tracks. Schemas-first approach. Quality hardening as systematic debt reduction. Review caught real UTF-8 bug. CI cross-compile correctly diagnosed. Progressive learning: 124m -> 28m -> 40m latest. Checkbox fidelity self-corrected in runs 17-18. -1: state leak identified but factory fix not applied |
| **Process** | 7/10 | Runs 2-8, 10-13, 17-18 flawless (39/39). Auto-cycling across 18 runs. Quality-hardening + audio-import showed corrected checkbox fidelity pattern. -3: state leak hit TWICE (same bug, never fixed — 4th flagging), build signal miss, live-ux required double recovery |

## Scoring Breakdown

| Dimension | Weight | Score | Weighted |
|-----------|--------|-------|----------|
| Efficiency | 25% | 8 | 2.00 |
| Stability | 20% | 6 | 1.20 |
| Fidelity | 20% | 9 | 1.80 |
| Quality | 15% | 10 | 1.50 |
| Commits | 5% | 10 | 0.50 |
| Docs | 5% | 9 | 0.45 |
| Signals | 5% | 7 | 0.35 |
| Speed | 5% | 2 | 0.10 |
| **Total** | 100% | | **7.9** |

**Score notes:**
- Efficiency 8: 6% waste (3/50 iters). Runs 2-13, 17-18 perfect, waste concentrated in Run 1 + Run 15.
- Stability 6: 2 state leak incidents (same unfixed bug), 1 global timeout, 1 build signal miss requiring restart.
- Fidelity 9: 99.3% criteria met in code across 16 tracks. -1: spec checkbox gap on live-ux (quality-hardening + audio-import fixed the pattern).
- Quality 10: 263/263 tests, build clean, clippy clean.
- Commits 10: 183/183 conventional.
- Docs 9: 14/16 tracks fully documented with SHAs. audio-import has full SHAs + checkboxes. Only live-ux + action-tracker outer still have unchecked specs.
- Signals 7: Clean signals, 1 miss (live-ux build). Runs 17-18 clean.
- Speed 2: ~17h total wall time (>8h). Per-track avg excellent at ~35 min. Latest run: 40 min.

## Recommendations

1. **[CRITICAL]** Fix state leak on timeout in `solo-dev.sh:939` — add state cleanup. **Fourth time flagging this.** Two tracks lost, recovered only via manual re-runs.

2. **[HIGH]** Deploy skill needs CLI/TUI project-type detection — auto-detect from Cargo.toml binary targets. Prevents AskUserQuestion stalls.

3. **[HIGH]** Deploy skill should check cross-compile compatibility — scan for native C deps before configuring CI targets.

4. **[MEDIUM]** Build skill should emit `<solo:done/>` signal earlier (after task completion, before optional cleanup) to prevent context exhaustion signal miss.

5. **[MEDIUM]** Add pre-flight validation to `solo-dev.sh` — check API key, rate limit status before main loop.

6. **[LOW]** Add `cargo-audit` in CI for dependency vulnerability scanning.

7. **[LOW]** Checkbox fidelity issue appears self-corrected — monitor in future runs but no urgent action needed.

## Suggested Patches

### Patch 1: solo-dev.sh:939 — Fix state leak on timeout (CRITICAL)

**What:** Clean state markers when global timeout prevents re-exec
**Why:** Two tracks silently skipped (action-tracker-v1, live-ux-v1). Same bug, never fixed. **Fourth** time flagged.

```diff
          log_entry "TIMEOUT" "Global timeout (${ELAPSED_H}h/${MAX_HOURS}h) — skipping re-exec"
+         # Clean state markers to prevent false completion on manual restart
+         rm -f "$STATES_DIR/build" "$STATES_DIR/deploy" "$STATES_DIR/review"
+         log_entry "TIMEOUT" "Cleaned state markers for safe restart"
        else
```

### Patch 2: deploy SKILL.md — CLI/TUI project detection

**What:** Auto-detect project type before deployment scope decisions
**Why:** Deploy stalled on AskUserQuestion for CLI/TUI project in Run 1

```diff
  ### 2. Detect available CLI tools
+ ### 2.5. Detect project type
+ Check CLAUDE.md and Cargo.toml for project type:
+ - Binary target + `ratatui`/`clap`/`crossterm` deps → CLI/TUI project
+ - CLI/TUI: deploy = GitHub repo + CI/CD + release workflow (no cloud hosting)
+ - Do NOT use AskUserQuestion for deployment scope — use sensible defaults
+
  Run in parallel — detect what's installed locally:
```

### Patch 3: deploy SKILL.md — Cross-compile check

**What:** Check native C dependencies before configuring CI targets
**Why:** ort-sys CI failure in whisper-stt deploy (26 min to diagnose)

```diff
  ### 3. Configure CI/CD
+ ### 2.7. Cross-compile compatibility check
+ Before configuring multi-arch CI targets, check for native C dependencies:
+ - Search Cargo.toml for `-sys` crates (e.g., ort-sys, openssl-sys)
+ - If found: verify prebuilt binaries exist for all target architectures
+ - If not available: limit CI to host architecture only
+
  Set up CI pipeline:
```

### Patch 4: build SKILL.md — Earlier signal emission

**What:** Emit progress signals at phase boundaries
**Why:** Context exhaustion in Run 15 caused signal miss at end of prompt

```diff
  ## Task Completion
+ After completing ALL tasks in a phase, emit a progress note.
+ If this is the FINAL phase: immediately emit `<solo:done/>` before any cleanup/summary.
+ Do not defer the signal to after documentation updates — emit it first.
+
  After completing ALL tasks in the current phase:
```

## Factory Critic

### Factory Score: 6.5/10 (unchanged from Run 17)

| Component | Score | Notes |
|-----------|-------|-------|
| /solo:build | 9/10 | 16/16 tracks delivered. Runs 17-18 had perfect checkboxes. -1: live-ux signal miss. |
| /solo:deploy | 7/10 | Self-corrected CLI/TUI handling. Two blind spots remain (project type, cross-compile). |
| /solo:review | 8/10 | Thorough reviews, correct SHIP/REDO verdicts. Runs 17-18 flawless. -2: live-ux checkbox miss. |
| /solo:plan (auto) | 10/10 | Generated 16 focused tracks from backlog. Progressive, well-scoped. audio-import was a good evolution. |
| solo-dev.sh | 4/10 | State leak defect at line 939 — hit TWICE, flagged **FOUR TIMES**, never fixed. Score unchanged. |

**Pipeline reliability:** 16/18 runs delivered code. 2 state leak incidents from same unfixed bug.

**Factory score held at 6.5** because:
1. audio-import was flawless (3/3 iters, checkbox fidelity correct, +7 tests)
2. Build + Review skills showed stable checkpoint behavior in runs 17-18
3. solo-dev.sh state leak **STILL UNFIXED** — remains the #1 factory defect

**Top factory defects:**
1. **CRITICAL** | `solo-dev.sh:939` — timeout doesn't clean state → 2 tracks silently skipped (4th flagging)
2. **HIGH** | `deploy SKILL.md` — no target compatibility check → CI failures on native C deps
3. **HIGH** | `deploy SKILL.md` — no CLI/TUI project-type detection → AskUserQuestion stalls
4. **MEDIUM** | `build SKILL.md` — signal emitted only at end → context exhaustion causes miss

### Harness Evolution

1. **Context engineering:** Excellent. CLAUDE.md lean at 14K. Schemas-first domain model. Plans always loaded at task boundaries (confirmed in audio-import iter-001). No context degradation across 18 runs. No scratch dir needed.

2. **Architectural constraints:** Perfect. Clean crate boundaries maintained (voxkit/agent/tui). audio-import correctly placed batch STT in voxkit, CLI in supervox-tui. No boundary violations.

3. **Decision traces:**
   - Good: Batch file transcription in voxkit, CLI orchestration in supervox-tui — correct layer placement
   - Good: OpenAI accepts raw bytes (no conversion), Whisper WAV-only with clear error — pragmatic format handling
   - Good: Copy (not move) audio to calls dir — preserves originals
   - Good: Auto-analyze after import (matches live flow) — consistent UX
   - Good: Checkbox fidelity self-corrected: quality-hardening and audio-import both perfect after live-ux gap
   - Bad: State leak bug identified in Run 9, recurred in Run 14, **still unfixed after 4 flaggings**

4. **Skill gaps:**
   - solo-dev.sh needs state cleanup on ALL exit paths (timeout, crash, manual stop) — CRITICAL
   - /deploy needs project-type detection + cross-compile check
   - /build needs earlier signal emission (progress checkpoints)
