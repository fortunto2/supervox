# Pipeline Retro: supervox (2026-03-20)

## Overall Score: 8.7/10

Pipeline completed Phase 1 (full workspace + agent + TUI) in 7 iterations with 1 redo cycle. Clean code quality, 100% conventional commits, all 84 tests passing.

## Pipeline Efficiency

| Metric | Value | Rating |
|--------|-------|--------|
| Total iterations | 7 | GREEN |
| Productive iterations | 5 (71.4%) | YELLOW |
| Wasted iterations | 2 (28.6%) | YELLOW |
| Pipeline restarts | 0 | GREEN |
| Max-iter hits | 0 | GREEN |
| Total duration | ~120 min | YELLOW |

## Per-Stage Breakdown

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 2 | 2 | 0% | Clean both runs |
| deploy | 3 | 2 | 33% | Iter 2: AskUserQuestion stall (CLI/TUI scope confusion) |
| review | 2 | 1 | 50% | Iter 4: valid redo — found UTF-8 bug, missing README, no hooks |

## Failure Patterns

### Pattern 1: Deploy Scope Confusion (CLI/TUI project)

- **Occurrences:** 1 iteration (iter 2)
- **Root cause:** Deploy skill designed for web/cloud deployments. Faced a CLI/TUI tool and couldn't determine deploy scope — asked AskUserQuestion which blocked the pipeline iteration. No `<solo:done/>` output.
- **Wasted:** 1 iteration (~3 min)
- **Fix:** Deploy skill should detect CLI/TUI projects from CLAUDE.md or Cargo.toml metadata and default to "GitHub repo + CI/CD + release workflow" without asking questions.

### Pattern 2: Doubled Signal (cosmetic)

- **Occurrences:** 1 (iter 4)
- **Root cause:** Review skill output `<solo:redo/>` twice — once inline and once in summary block. Matches catalog Pattern 4.
- **Wasted:** 0 iterations (no impact)
- **Fix:** None needed. Cosmetic only.

### Pattern 3: Valid Redo Cycle

- **Occurrences:** 1 redo cycle (iter 4→5→6→7)
- **Root cause:** Review correctly identified 3 real issues (UTF-8 slicing bug, missing README, no pre-commit hooks). Redo was productive — all 3 issues fixed in iter 5.
- **Wasted:** 0 (the redo was justified and the fix iteration was efficient — 3 min)
- **Assessment:** This is the redo mechanism working as designed.

## Plan Fidelity

| Track | Criteria Met | Tasks Done | SHAs | Rating |
|-------|-------------|------------|------|--------|
| phase1 | 13/13 (100%) | 20/20 (100%) | 20/20 | GREEN |
| phase2 | 0/9 (0%) | 0/8 (0%) | 0/8 | N/A (not in scope) |
| phase3 | 0/8 (0%) | — | — | N/A (not in scope) |
| phase4 | 0/10 (0%) | — | — | N/A (not in scope) |

Phase 1 fully delivered. Phases 2-4 remain for future pipeline runs.

## Code Quality (Quick)

- **Tests:** 84 pass, 0 fail (33 agent + 6 tui + 6 agent-extra + 39 voxkit)
- **Build:** PASS
- **Clippy:** PASS (1 warning in external dep sgr-agent, not in supervox workspace)
- **Fmt:** PASS
- **Commits:** 27 total, 100% conventional format
- **Pre-commit hooks:** Active (cargo fmt + clippy)

## Three-Axis Growth

| Axis | Score | Evidence |
|------|-------|----------|
| **Technical** (code, tools, architecture) | 9/10 | Full Rust workspace with 3 crates, 6 agent tools, TUI with 3 modes, all types with serde/schemars, proper error handling |
| **Cognitive** (understanding, strategy, decisions) | 8/10 | Good architectural decisions: voxkit reuse, sgr-agent-tui integration, schemas-first approach. PRD→TUI pivot was handled cleanly |
| **Process** (harness, skills, pipeline, docs) | 7/10 | Pipeline ran smoothly overall but deploy skill stumbled on CLI/TUI scope. Review→redo cycle was productive. CLAUDE.md stayed lean (4,251 bytes) |

## Scoring Breakdown

| Dimension | Weight | Score | Weighted |
|-----------|--------|-------|----------|
| Efficiency | 25% | 6 | 1.50 |
| Stability | 20% | 10 | 2.00 |
| Fidelity | 20% | 10 | 2.00 |
| Quality | 15% | 10 | 1.50 |
| Commits | 5% | 10 | 0.50 |
| Docs | 5% | 10 | 0.50 |
| Signals | 5% | 7 | 0.35 |
| Speed | 5% | 6 | 0.30 |
| **Total** | 100% | | **8.65 → 8.7** |

## Recommendations

1. **[HIGH]** Deploy skill needs CLI/TUI project awareness — should auto-detect from `Cargo.toml` binary targets or CLAUDE.md "TUI"/"CLI" keywords and default to GitHub CI/CD + release workflow without AskUserQuestion.

2. **[MEDIUM]** Review signal output had doubled `<solo:redo/>` — consider adding dedup in review skill's signal section (output once, not in both code block and summary).

3. **[LOW]** Consider adding `cargo-audit` to CI for dependency vulnerability scanning.

4. **[LOW]** Consider `cargo-tarpaulin` for coverage reporting in CI.

## Suggested Patches

### Patch 1: Deploy SKILL.md — Add CLI/TUI project detection

**What:** Add pre-flight check for CLI/TUI projects to avoid AskUserQuestion stalls
**Why:** Deploy iter 2 wasted asking user about scope when it could auto-detect from project metadata

```diff
  ### 2. Detect available CLI tools
+ ### 2.5. Detect project type
+ Check CLAUDE.md and Cargo.toml for project type indicators:
+ - If binary target (not lib), has `ratatui`/`clap`/`crossterm` deps → CLI/TUI project
+ - If CLAUDE.md contains "TUI", "CLI", "terminal" → CLI/TUI project
+ - For CLI/TUI projects: deploy = GitHub repo + CI/CD + release workflow + `make install`
+ - Do NOT ask user about deployment scope for CLI/TUI — use sensible defaults
+
  Run in parallel — detect what's installed locally:
```

## Factory Score: 8/10

### Skill quality:
- **build:** 10/10 — Executed Phase 1 flawlessly. 20 tasks, all with SHA annotations. Handled multi-phase plan correctly.
- **deploy:** 6/10 — Failed on first attempt for CLI/TUI project. AskUserQuestion blocked pipeline. Recovered on retry by setting up CI/CD correctly. Needs project-type awareness.
- **review:** 9/10 — Found 3 real issues (UTF-8 bug was a genuine security-adjacent fix). Redo was justified. Only deduction: doubled signal output.

### Pipeline reliability: 9/10
Single clean run, no restarts, no maxiter hits. Redo mechanism worked correctly. State management was clean.

### Missing capabilities:
- No CLI/TUI-specific deploy strategy (binary releases, homebrew tap, cargo install)
- No visual regression testing for TUI apps (snapshot-based)

### Top factory defects:
1. Deploy skill lacks project-type detection → `skills/deploy/SKILL.md` → Add CLI/TUI auto-detection section
2. Review doubled signal output → `skills/review/SKILL.md` → Minor, cosmetic

### Harness Evolution

**Context engineering:** Agent had everything it needed. CLAUDE.md was well-structured with tech stack, dependencies, and key decisions. Schemas-first approach ensured domain types were defined before implementation.

**Architectural constraints:** No boundary violations detected. Agent followed clean architecture (types → tools → storage → TUI modes). File sizes stayed under limits.

**Decision traces:**
- Good: voxkit reuse avoided audio code duplication
- Good: sgr-agent-tui ChatPanel reuse gave agent mode for free
- Good: Review-initiated redo fixed a real UTF-8 bug before shipping

**Skill gaps:**
- Deploy needs Rust-native deployment awareness (cargo publish, binary releases, cross-compilation)
- No TUI-specific testing skill (screen capture, key sequence testing)
