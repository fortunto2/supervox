# Pipeline Retro: supervox (2026-03-20) — Complete Pipeline

## Overall Score: 8.9/10

Four pipeline runs in one continuous session: Phase 1 (workspace + agent + TUI skeleton, 7 iters, 124 min), realtime-live (streaming STT + live intelligence, 3 iters, 32 min), analysis-agent-wire (LLM wiring for analysis + agent modes, 3 iters, 28 min), ux-polish (help overlay, history browser, JSON output, Ollama config, 3 iters, 28 min). 16 total iterations, 87.5% productive, 128 tests passing, 65 commits all conventional.

## Pipeline Efficiency

| Metric | Value | Rating |
|--------|-------|--------|
| Total iterations | 16 (7 + 3 + 3 + 3) | GREEN |
| Productive iterations | 14 (87.5%) | GREEN |
| Wasted iterations | 2 (12.5%) | GREEN |
| Pipeline runs | 4 (1 manual + 3 auto-restarts) | GREEN |
| Aborted pre-starts | 6 (earlier log files) | YELLOW |
| Max-iter hits | 0 | GREEN |
| Total duration | ~212 min (124 + 32 + 28 + 28) | YELLOW |

### Pre-Start Failures

Before the main pipeline, 6 aborted starts occurred across `pipeline-20260320-013628.log` (3 STARTs, no iters) and `pipeline-20260320-014937.log` (3 STARTs + rate limit). These were setup/configuration attempts — no iterations burned, but indicate initial friction.

## Per-Stage Breakdown

### Run 1: Phase 1 (7 iterations, 124 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 2 | 2 | 0% | Clean both runs (47 min initial, 3 min fix) |
| deploy | 3 | 2 | 33% | Iter 2: AskUserQuestion stall (CLI/TUI scope confusion) |
| review | 2 | 1 | 50% | Iter 4: valid redo — found UTF-8 bug, missing README, no hooks |

### Run 2: realtime-live (3 iterations, 32 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 1 | 1 | 0% | 22 min, all 4 phases of plan completed in single build |
| deploy | 1 | 1 | 0% | 3 min, clean push + archive |
| review | 1 | 1 | 0% | 2 min, clean pass with spec verification |

### Run 3: analysis-agent-wire (3 iterations, 28 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 1 | 1 | 0% | 16 min, 4-phase plan (analysis + clipboard + agent + docs) |
| deploy | 1 | 1 | 0% | 3 min, Cargo.lock + plan archive + push |
| review | 1 | 1 | 0% | 3 min, clean pass — all 10 acceptance criteria verified |

### Run 4: ux-polish (3 iterations, 28 min)

| Stage | Attempts | Successes | Waste % | Notes |
|-------|----------|-----------|---------|-------|
| build | 1 | 1 | 0% | 22 min, 4 phases (help overlay + history browser + CLI + docs) |
| deploy | 1 | 1 | 0% | 3 min, version bump + Cargo.lock update |
| review | 1 | 1 | 0% | 2 min, clean pass — all 8 acceptance criteria verified |

Runs 2, 3, and 4 were **flawless** — 9/9 productive, zero waste. Auto-plan from backlog picked correct priorities: realtime-live → analysis-agent-wire → ux-polish. Each successive run was faster and cleaner.

## Failure Patterns

### Pattern 1: Deploy Scope Confusion (CLI/TUI project)

- **Occurrences:** 1 iteration (Run 1, iter 2)
- **Root cause:** Deploy skill designed for web/cloud deployments. Faced a CLI/TUI tool and used AskUserQuestion which blocked the pipeline. No `<solo:done/>` output.
- **Wasted:** 1 iteration (~3 min)
- **Fix:** Deploy skill needs project-type detection: binary targets + TUI/CLI keywords → auto CI/CD + release workflow.
- **Note:** Deploy learned from failure — Runs 2, 3, and 4 completed cleanly (3 min each).

### Pattern 2: Doubled Signal (cosmetic)

- **Occurrences:** 1 (Run 1, iter 4)
- **Root cause:** Review skill output `<solo:redo/>` twice. Matches catalog Pattern 4.
- **Wasted:** 0 iterations
- **Fix:** None needed.

### Pattern 3: Valid Redo Cycle

- **Occurrences:** 1 redo cycle (Run 1, iter 4→5→6→7)
- **Root cause:** Review correctly identified 3 real issues: UTF-8 slicing bug in search (would panic on Cyrillic), missing README, no pre-commit hooks. All fixed in 3 min.
- **Assessment:** Redo mechanism working exactly as designed. Build skill completed 3 review-requested tasks with SHA annotations.

### Pattern 4: Pre-Start Rate Limit

- **Occurrences:** 1 (pipeline-20260320-014937.log)
- **Root cause:** Claude CLI rate-limited during pipeline startup.
- **Wasted:** ~1 min wait time
- **Note:** Rate limit detection in solo-dev.sh worked correctly.

## Plan Fidelity

| Track | Criteria Met | Tasks Done | SHAs | Rating |
|-------|-------------|------------|------|--------|
| phase1 | 13/13 (100%) | 20/20 (100%) | 20/20 | GREEN |
| realtime-live | 9/9 (100%) | 14/14 (100%) | 14/14 | GREEN |
| analysis-agent-wire | 10/10 (100%) | 17/17 (100%) | 17/17 | GREEN |
| ux-polish | 8/8 (100%) | 15/15 (100%) | 15/15 | GREEN |
| phase2 | -- | -- | -- | Archived (replaced by realtime-live) |
| phase3 | -- | -- | -- | Archived (replaced by analysis-agent-wire) |
| phase4 | -- | -- | -- | Archived (replaced by ux-polish) |

All four executed tracks: 100% criteria met, 100% tasks done (66/66), all with SHA annotations. Original phases 2-4 were broad plans superseded by focused auto-generated tracks.

## Code Quality (Quick)

- **Tests:** 128 pass, 0 fail, 2 ignored (36 agent + 50 tui + 42 voxkit)
- **Build:** PASS
- **Clippy:** PASS (1 warning in external dep sgr-agent, not supervox)
- **Fmt:** PASS
- **Commits:** 65 total, 100% conventional format
- **Pre-commit hooks:** Active (cargo fmt + clippy)
- **CLAUDE.md:** 7,532 bytes (well under 40K limit)

## Three-Axis Growth

| Axis | Score | Evidence |
|------|-------|----------|
| **Technical** | 10/10 | 3 Rust crates, 6 agent tools, realtime WebSocket STT, system audio capture, async translation + summary + analysis pipelines, agent mode with streaming LLM, clipboard, config system, help overlay, call history browser, Ollama support, JSON CLI output, error resilience |
| **Cognitive** | 10/10 | Auto-plan evolved plans from broad phases to focused tracks (3 successful evolutions). Schemas-first approach. Review caught real UTF-8 bug. voxkit reuse avoided duplication. Each auto-plan examined completed work and generated the optimal next track. |
| **Process** | 10/10 | Runs 2-4 all flawless (9/9 productive). Pipeline auto-cycling worked across 4 runs. Each successive run was faster and cleaner. Progressive improvement: 71% → 100% → 100% → 100% productive. |

## Scoring Breakdown

| Dimension | Weight | Score | Weighted |
|-----------|--------|-------|----------|
| Efficiency | 25% | 7 | 1.75 |
| Stability | 20% | 10 | 2.00 |
| Fidelity | 20% | 10 | 2.00 |
| Quality | 15% | 10 | 1.50 |
| Commits | 5% | 10 | 0.50 |
| Docs | 5% | 10 | 0.50 |
| Signals | 5% | 9 | 0.45 |
| Speed | 5% | 4 | 0.20 |
| **Total** | 100% | | **8.90 → 8.9** |

**Score notes:**
- Efficiency 7: 12.5% waste (2/16 iters) — improved from 15.4% (2/13) with Run 4's clean execution
- Stability 10: 0 failure-restarts, 0 maxiter. 4 STARTs are sequential auto-plan runs, not failure recovery
- Signals 9: Runs 2-4 had clean signals. Only Run 1 had iter-2 missing signal and doubled redo
- Speed 4: ~212 min total (~3.5 hours). Individual runs got faster: 124m → 32m → 28m → 28m

## Recommendations

1. **[HIGH]** Deploy skill needs CLI/TUI project-type detection — auto-detect from Cargo.toml binary targets + CLAUDE.md keywords, default to GitHub CI/CD + release workflow. Prevents AskUserQuestion stalls.

2. **[MEDIUM]** Pre-start friction — 6 aborted starts before successful pipeline. Consider retry/backoff in solo-dev.sh startup phase or clearer error messages for config issues.

3. **[LOW]** Add `cargo-tarpaulin` or `cargo-llvm-cov` for test coverage metrics in CI.

4. **[LOW]** Consider splitting the 128-test suite into `make test-fast` (unit) and `make test-full` (integration) for faster iteration.

## Suggested Patches

### Patch 1: Deploy SKILL.md — CLI/TUI project detection

**What:** Add project-type detection before deployment to handle non-web projects
**Why:** Deploy iter 2 wasted on AskUserQuestion for CLI/TUI scope

```diff
  ### 2. Detect available CLI tools
+ ### 2.5. Detect project type
+ Check CLAUDE.md and Cargo.toml for project type:
+ - Binary target + `ratatui`/`clap`/`crossterm` deps -> CLI/TUI project
+ - CLAUDE.md contains "TUI", "CLI", "terminal" -> CLI/TUI project
+ - CLI/TUI projects: deploy = GitHub repo + CI/CD + release workflow + binary asset
+ - Do NOT use AskUserQuestion for deployment scope — use sensible defaults
+
  Run in parallel — detect what's installed locally:
```

## Factory Score: 9.0/10

### Skill quality:

| Skill | Score | Run 1 | Run 2 | Run 3 | Run 4 |
|-------|-------|-------|-------|-------|-------|
| build | 10/10 | 20 tasks, all SHAs | 14 tasks, 4 phases, single iter | 17 tasks, 16 min | 15 tasks, 22 min, 4 phases |
| deploy | 8/10 | Failed iter 2 (stall) | Clean 3 min | Clean 3 min | Clean 3 min |
| review | 10/10 | Valid redo — caught bugs | Clean pass | Clean pass, all criteria | Clean pass, all criteria |

Deploy improved from 7/10 (Run 1 only) to 8/10 — consistently clean in Runs 2-4. Build and Review both perfect across all 4 runs.

### Pipeline reliability: 10/10

Four successful runs with auto-plan cycling. Zero maxiter hits. Auto-plan correctly selected priorities: realtime-live → analysis-agent-wire → ux-polish. State management clean throughout.

### Auto-plan quality: 10/10

Auto-plan examined completed tracks + codebase and generated focused plans that adapted to what was actually needed. Three successful evolutions — each better-targeted than running the original broad phases.

### Missing capabilities:
- Rust-native deploy strategy (cargo publish, homebrew tap, cross-compilation)
- TUI visual regression testing (ratatui snapshot tests)
- Pre-start validation in solo-dev.sh

### Top factory defects:
1. Deploy skill lacks project-type detection → `skills/deploy/SKILL.md` → Add CLI/TUI auto-detection
2. Pre-start instability → `scripts/solo-dev.sh` → Add startup validation + clearer error messages

### Harness Evolution

**Context engineering:** Agent had everything needed in-repo. CLAUDE.md grew from 5,375 to 7,532 bytes as features were documented — lean and actionable. Schemas-first gave clear domain model. Auto-plan had full visibility into plan-done archives + PRD + codebase.

**Architectural constraints:** No boundary violations across all 4 runs. Agent maintained clean separation: types → tools → storage → audio pipeline → intelligence → analysis_pipeline → agent_loop → TUI modes. All files under limits.

**Decision traces:**
- Excellent: Auto-plan evolving from broad phases to focused tracks (3 successful evolutions)
- Excellent: voxkit reuse — zero audio code duplication across all 4 runs
- Excellent: Wiring strategy (existing tools → async pipelines → app events → UI) was clean and consistent
- Excellent: ux-polish combined 4 high-impact items into one focused track instead of scattering them
- Good: Review redo in Run 1 caught real UTF-8 bug before shipping
- Good: Progressive test growth (84 → 110 → 128) with each track adding relevant tests

**Skill gaps:**
- Deploy needs Rust/CLI/TUI awareness
- No TUI snapshot testing capability
- Auto-plan pattern is strong — should be documented as reference for other projects
