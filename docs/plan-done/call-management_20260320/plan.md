# Implementation Plan: Call Management & Export

**Track ID:** call-management_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

Add delete, export, and CLI search to make call history manageable. Three phases: storage layer + tests, CLI commands, TUI integration.

## Phase 1: Storage Layer — Delete & Export <!-- checkpoint:93ec640 -->

Add `delete_call()` and `export_call_markdown()` to `supervox-agent/src/storage.rs`. TDD — tests first.

### Tasks
- [x] Task 1.1: Add `delete_call(calls_dir, call_id)` to `crates/supervox-agent/src/storage.rs` — find file by ID suffix (same pattern as `load_call`), remove it. Return Ok/Err. <!-- sha:94d34a5 -->
- [x] Task 1.2: Add `export_call_markdown(call, analysis)` to `crates/supervox-agent/src/storage.rs` — format Call + optional CallAnalysis as markdown string. Include date, duration, participants, transcript, translation, and all analysis sections. <!-- sha:bb5e691 -->
- [x] Task 1.3: Write tests in `crates/supervox-agent/src/storage.rs` — `delete_call_removes_file`, `delete_nonexistent_fails`, `export_markdown_full`, `export_markdown_no_analysis`, `export_markdown_with_translation`. <!-- sha:48a8143 -->

### Verification
- [x] `cargo test -p supervox-agent` passes with new tests
- [x] `delete_call` removes file and `load_call` fails after
- [x] `export_call_markdown` produces valid markdown with all sections

## Phase 2: CLI Commands — Delete, Export, Search <!-- checkpoint:4596872 -->

Wire storage functions to CLI subcommands in `crates/supervox-tui/src/main.rs`.

### Tasks
- [x] Task 2.1: Add `Delete { call_id, force }` subcommand to `crates/supervox-tui/src/main.rs` — load call to confirm it exists, print call summary, prompt for confirmation (unless `--force`), call `delete_call()`. <!-- sha:1288cd5 -->
- [x] Task 2.2: Add `Export { call_id, output }` subcommand to `crates/supervox-tui/src/main.rs` — load call, optionally run analysis (via `analyze_transcript`), format with `export_call_markdown()`, write to file or stdout. <!-- sha:1288cd5 -->
- [x] Task 2.3: Add `Search { query, json }` subcommand to `crates/supervox-tui/src/main.rs` — use `supervox_agent::tools::search::search_calls()` to find matches, display results with call date + matching snippet. Support `--json` for machine output. <!-- sha:1288cd5 -->
- [x] Task 2.4: Write CLI integration tests in `crates/supervox-tui/tests/` or inline — test delete with force flag, export output format, search result format. <!-- sha:4596872 -->

### Verification
- [x] `supervox delete <id> --force` removes call file
- [x] `supervox export <id>` outputs markdown to stdout
- [x] `supervox search <query>` shows matching calls with context
- [x] `cargo test -p supervox-tui` passes with new tests

## Phase 3: TUI Integration — Delete in History + Export in Analysis <!-- checkpoint:e581d2d -->

Add keyboard shortcuts for delete and export in existing TUI modes.

### Tasks
- [x] Task 3.1: Add 'd' key handler in `crates/supervox-tui/src/app.rs` `handle_history_key()` — show confirmation in status bar ("Delete call? y/n"), wait for y/n, call `delete_call()`, refresh call list, adjust cursor. <!-- sha:e581d2d -->
- [x] Task 3.2: Add `confirm_delete` state to `crates/supervox-tui/src/modes/history.rs` `CallHistoryState` — tracks pending delete confirmation. <!-- sha:e581d2d -->
- [x] Task 3.3: Add 'e' key handler in `crates/supervox-tui/src/modes/analysis.rs` `handle_key()` — build markdown from current analysis state, copy to clipboard via `arboard`, show status "Analysis exported to clipboard". <!-- sha:e581d2d -->
- [x] Task 3.4: Update `crates/supervox-tui/src/help.rs` — add 'd' (delete) to History mode help, add 'e' (export) to Analysis mode help. <!-- sha:e581d2d -->
- [x] Task 3.5: Write TUI tests — delete confirmation state transitions, export markdown clipboard content. <!-- sha:e581d2d -->

### Verification
- [x] 'd' in History shows confirmation, 'y' deletes, 'n' cancels
- [x] 'e' in Analysis copies markdown to clipboard
- [x] Help overlay shows new keybindings
- [x] `cargo test --workspace` passes

## Phase 4: Docs & Cleanup <!-- checkpoint:41ea39c -->

### Tasks
- [x] Task 4.1: Update CLAUDE.md with new CLI commands (`delete`, `export`, `search`), new keybindings ('d' in History, 'e' in Analysis) <!-- sha:41ea39c -->
- [x] Task 4.2: Update README.md with new commands and keybindings <!-- sha:41ea39c -->
- [x] Task 4.3: Remove dead code — unused imports, orphaned files, stale exports <!-- sha:41ea39c -->

### Verification
- [x] CLAUDE.md reflects current project state
- [x] Linter clean, tests pass

## Final Verification

- [x] All acceptance criteria from spec met
- [x] Tests pass
- [x] Linter clean
- [x] Build succeeds
- [x] Documentation up to date

---
_Generated by /plan. Tasks marked [~] in progress and [x] complete by /build._
