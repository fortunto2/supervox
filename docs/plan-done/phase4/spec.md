# Phase 4 — Acceptance Criteria

- Help overlay shows all keybindings per mode
- Call history browser navigable with keyboard
- Ollama local LLM works as alternative backend
- Speaker labels (You/Caller) in live transcript
- Audio waveform visible during recording
- `--json` flag works for CLI subcommands
- Graceful error handling (no crashes on API failures)
- README.md with installation + usage
- All tests pass, clippy clean
- Tagged as v0.1.0
